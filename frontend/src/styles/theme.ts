import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#259925",
    },
    background: {
      default: "#fffafa",
    },
    text: { primary: "#333232" },
  },
});

export default theme;
